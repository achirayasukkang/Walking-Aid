import serial

ser = serial.Serial('/dev/ttyUSB0', 9600)  # ปรับตามพอร์ตที่ใช้งาน

while True:
    if ser.in_waiting > 0:
        line = ser.readline().decode('utf-8').rstrip()
        print(line)
        # ส่งข้อมูลไปยัง socket.io
        socketio.emit('data', {'message': line})

//py
